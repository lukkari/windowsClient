<Page
    x:Class="Schedule.UniversalApp.WeekSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Schedule.UniversalApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{Binding WeekSelector, Mode=OneWay, Source={StaticResource Locator}}">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Pivot x:ConnectionId='1' x:Name="WeekSelectorPivot" Title="Select week in:"                                  >
            <Pivot.Resources>
                <DataTemplate x:Key="WeeksListBoxTemplate">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Week" TextWrapping="Wrap" FontSize="{ThemeResource TextStyleExtraLargeFontSize}" Foreground="{ThemeResource PhoneForegroundBrush }"></TextBlock>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="{ThemeResource TextStyleExtraLargeFontSize}" Foreground="{ThemeResource PhoneForegroundBrush }"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </Pivot.Resources>
            <PivotItem x:Name="Autumn" Header="autumn">
                <ListBox ItemTemplate="{StaticResource ResourceKey= WeeksListBoxTemplate}" ItemsSource="{Binding WeekSchedulesAutumn.Keys}" Background="{ThemeResource PhoneBackgroundBrush}" SelectedItem="{Binding SelectedWeek , Mode=TwoWay}"></ListBox>
            </PivotItem>
            <PivotItem x:Name="Spring" Header="spring">
                <ListBox ItemTemplate="{StaticResource ResourceKey= WeeksListBoxTemplate}" ItemsSource="{Binding WeekSchedulesSpring.Keys}" Background="{ThemeResource PhoneBackgroundBrush}" SelectedItem="{Binding SelectedWeek, Mode=TwoWay}"></ListBox>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>

